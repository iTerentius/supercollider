$: n(run(4)).s("DrumLoops")
  // .chop("2 4 8")
  // .slow(sine.add(1).mul(2))
  .begin(saw)
  .end(isaw.late(0.5))
  .gain(0.9)
  .every(2,jux(rev))
  // .degradeBy(isaw)
  .cut(3)
  .orbit(0)

_$: note("e2 e b e4 c2 g c g a2 g a b2 g2".slow(4))
.s("st_fm3")
// .f0ratio(rand.range(3,5))
// .f1ratio(rand.range(1,3))
// .modInd0(rand.range(0,2.0))
// .modInd1(rand.range(0,1.0))
// .delay("1.5:1:0.3")
// .room(0.7).rsize(0.4).roomfade(2)
.orbit(4)

_$:
n("0!16".add(brandBy(0.5).fast(4).mul("2 4 6 8"))).scale("d:dorian").s("st_fm3")
// .mask("1(4,7)")

// $: "hh".s().struct(binary(11))
