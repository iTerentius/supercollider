// Bass
(
Pdef(\bass,
    Pbindef(\bass_pbd,
        \type, \midi, \midiout, ~mOut, \chan, ~cfg.ch.bass,
        \root, Pkey(\root),
        \scale, Pkey(\scale),
        \octave, 3,
        \degree, 0,
        \amp, 0.6,
        \dur, 1,
    )
);
)
